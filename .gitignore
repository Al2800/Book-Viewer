# bv (beads viewer) local config and caches
.bv/

# MCP Agent Mail local SQLite (agents/messages/reservations) — never commit
storage.sqlite3
storage.sqlite3-shm
storage.sqlite3-wal
storage.sqlite3-*

# XcodeBuildMCP local config
.xcodebuildmcp/

# Xcode user-specific state (should never be committed)
*.xcuserstate
*.xcuserdata
**/xcuserdata/

# Derived artifacts created by scripts and builds
artifacts/
build/
TestResults/
.DerivedData/
.DerivedData-prodcheck/
.DerivedData-capturecheck/
.DerivedData-cameraharmonize/
backend/node_modules/

# Local-only real book/photo fixtures. Keep copyrighted pages out of the public repo.
local-fixtures/
